/// Bumped by hand on every push so the running build can be eyeballed on
/// screen (Chrome tabs / dev servers can otherwise silently serve stale JS).
const String kAppBuildLabel = 'v1.3.23 beta';

/// Shown once, in a dialog, the first time this build label is seen.
const String kAppBuildNote =
    'Android uygulaması artık açılışta yeni bir sürüm olup olmadığını '
    'GitHub\'dan sessizce kontrol ediyor; varsa haritanın üstünde bir '
    '"Güncelle" düğmesi çıkıyor ve tek dokunuşla indirme sayfasını açıyor.';
