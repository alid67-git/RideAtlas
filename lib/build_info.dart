/// Bumped by hand on every push so the running build can be eyeballed on
/// screen (Chrome tabs / dev servers can otherwise silently serve stale JS).
const String kAppBuildLabel = 'v1.3.45 beta';

/// Shown once, in a dialog, the first time this build label is seen.
const String kAppBuildNote =
    'Kayıt ekranındaki harita artık normal navigasyon uygulamaları gibi '
    'varsayılan olarak yön-takip (course-up) modunda açılıyor. Konum '
    'tuşuna basarak kuzey-sabit moda geçebilirsiniz; hangi modu seçtiyseniz '
    'bir sonraki kayıtta da o hatırlanır.';
