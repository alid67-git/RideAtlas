/// Bumped by hand on every push so the running build can be eyeballed on
/// screen (Chrome tabs / dev servers can otherwise silently serve stale JS).
const String kAppBuildLabel = 'v1.3.36 beta';

/// Shown once, in a dialog, the first time this build label is seen.
const String kAppBuildNote =
    'REC göstergesi artık ayarlar ikonunun üstüne binmiyor - satırın bir '
    'parçası olarak ayarların sağına oturuyor. Rota haritasının üst '
    'çubuğundaki foto ekleme ikonu kaldırıldı (alttaki şeritte zaten var).';
