/// Bumped by hand on every push so the running build can be eyeballed on
/// screen (Chrome tabs / dev servers can otherwise silently serve stale JS).
const String kAppBuildLabel = 'v1.3.29 beta';

/// Shown once, in a dialog, the first time this build label is seen.
const String kAppBuildNote =
    'Rota isminin altında artık ayın ve yılın (gün olmadan) yazdığı bir '
    'tarih gösteriliyor. Dosya adından gelen rota isimlerinin sonundaki '
    'gereksiz alt çizgi/tire gibi karakterler artık otomatik temizleniyor.';
