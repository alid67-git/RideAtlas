/// Bumped by hand on every push so the running build can be eyeballed on
/// screen (Chrome tabs / dev servers can otherwise silently serve stale JS).
const String kAppBuildLabel = 'v0.1.8';

/// Shown once, in a dialog, the first time this build label is seen.
const String kAppBuildNote =
    'Analiz paneli artık ortada, derli toplu bir pencere olarak açılıyor '
    '(alttan açılan panel yerine). Harita ekranındaki liste butonu da artık '
    'geçerli haritayı kapatmadan diğer rotaların listesini ortada bir pencerede '
    'açıyor; seçmezsen Esc/dışarı tıklayarak aynı haritaya dönebilirsin.';
